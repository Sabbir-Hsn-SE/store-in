import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: [
      "i.imgur.com",
      "placehold.co",
      "test.com",
      "pravatar.cc",
      "images.unsplash.com",
      "ybmq92bflj.ufs.sh",
    ],
  },
};

export default nextConfig;
